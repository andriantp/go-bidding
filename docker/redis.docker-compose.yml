services:
  redis:
    image: redis:7-alpine
    container_name: redis-auction
    ports:
      - "6380:6379"
    command: >
      redis-server
      --appendonly yes
    volumes:
      - ./redis-data:/data
    restart: unless-stopped